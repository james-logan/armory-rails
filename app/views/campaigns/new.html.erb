<h2>New Campaign</h2>

<%= form_for @campaign do |f| %>
  <p>
    <%= f.label(:title) %>
    <%= f.text_field(:title) %>
  </p>
  <p>
    <%= f.label(:description) %>
    <%= f.text_area(:description) %>
  </p>

  <div>
    <%= f.submit %>
  </div>
<% end %>
